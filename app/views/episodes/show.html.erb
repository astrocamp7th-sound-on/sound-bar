<%= link_to "返回單集列表", podcast_path(@podcast) %>

<h1><%= @episode.title %></h1>
<ul>
  <li>關鍵字：<%= @episode.keyword %></li>
  <li>描述：<%= @episode.description %></li>
</ul>


<p>
   <strong>Cover:</strong>
   <%= image_tag(@episode.cover_url) if @episode.cover? %>
 </p>